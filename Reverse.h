#ifndef REVERSE_H
#define REVERSE_H
#include <iostream>
#include <string>

class Reverse {
    public:
    Reverse();
    int reverseDigit(int digit);
    std::string reverseString(std::string str);
};
#endif